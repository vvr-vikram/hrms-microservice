server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: ${CONFIG_REPO_PATH:file:///C:/Users/Vigneshxbs/eclipse-workspace/hrms-microservice/hrms-microservice/config-repo}

management:
  endpoints:
    web:
      exposure:
        include: health
